import { JsonRpc } from './websocket';
export declare const OASIS_CHAIN_ID = 42261;
export declare class TransactionFactory {
    private address;
    private rpc;
    constructor(address: string, rpc: JsonRpc);
    create(tx: UnpreparedTransaction): Promise<Transaction>;
    estimateGas(tx: Record<string, any>): Promise<any>;
    nonce(): Promise<any>;
}
/**
 * Transaction that might need fields to be filled in, e.g., via estimateGas.
 */
export declare type UnpreparedTransaction = {
    to?: string;
    value?: string;
    data?: string;
    nonce?: string;
    gasLimit?: string;
    gasPrice?: string;
    chainId?: number;
};
export declare type Transaction = {
    to?: string;
    value: string;
    data: string;
    nonce: string;
    gasLimit: string;
    gasPrice: string;
    chainId: number;
};
